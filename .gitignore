# Build artifacts
youty.app/
build/
DerivedData/
*.xcuserstate
youty-mcp/dist/
youty-mcp/.venv/
*.dmg
*.whl

# Xcode
xcuserdata/
*.xcworkspace/xcuserdata/

# Debug logs
.playwright-mcp/

# EmbeddingGemma raw tokenizer source — 32 MB build input fetched from HuggingFace.
# Only the compact vocab/merges/added_tokens .bin artifacts are committed + shipped
# (built by Scripts/build-gemma-tokenizer-artifact.py).
Vendor/embeddinggemma/tokenizer/tokenizer.json

# macOS
.DS_Store

# Secrets — never commit any of these.
# Defence-in-depth before Phase R (public repo).
.env
.env.*
*.key
*.pem
*.p12
secrets/
.secrets/
.gemini-api.key
